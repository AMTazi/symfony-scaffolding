{{ form_start(form) }}

    <div class="modal-body">
        {{ form_widget(form) }}
    </div>

    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal" onclick="return false">{{ 'Close'|trans()}}</button>
        <button class="btn btn-primary">{{ 'Save'|trans()}}</button>
    </div>

{{ form_end(form) }}



